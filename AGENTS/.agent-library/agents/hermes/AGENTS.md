# Hermes Agent Adapter

Before any task: read GLOBAL_AGENT_BOOT.md, run skill router, check Fable mode.

When delegating to workers: pass AGENTS.md and selected skills into worker prompts.

After worker returns: verify claims against files/logs, report evidence.

Use native model unless user explicitly overrides. Report model used per worker.
